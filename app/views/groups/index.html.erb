<h1><%= @community.name %> Lunch Groups</h1>

<div id="groups" class="row">
  <%= render @groups %>
</div>

<% unless @ungroupedRestaurants.empty? %>
  <%= render 'form', community: @community, availableRestaurants: @ungroupedRestaurants %>
<% else %>
  <%# TODO: There should be the ability to create a restaurant from here!! %>
<% end %>